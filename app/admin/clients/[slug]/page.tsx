// export default async function DynamicClient({
//   params,
// }: {
//   params: Promise<{ client: string }>;
// }) {
//   const slug = (await params).client;
//   console.log(slug);

//   return <div>My Post: {slug}</div>;
// }
